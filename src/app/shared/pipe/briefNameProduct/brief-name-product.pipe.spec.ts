import { BriefNameProductPipe } from './brief-name-product.pipe';

describe('BriefNameProductPipe', () => {
  it('create an instance', () => {
    const pipe = new BriefNameProductPipe();
    expect(pipe).toBeTruthy();
  });
});
